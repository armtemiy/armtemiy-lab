# Armtemiy Lab v2.0 — План-спецификация

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                      TELEGRAM BOT                           │
│  • Профиль пользователя (подписка, история, данные)        │
│  • Инфо (контакты + статистика техник)                     │
│  • Получение видео техники после подбора в WebApp          │
│  • Кнопка → WebApp                                          │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                      WEBAPP (React)                         │
│  • Разбор борьбы (алгоритм)                                │
│  • Подбор техники (антропометрия + алгоритм)               │
│  • Поиск спарринг-партнёров (карта + профили)              │
│  • Библиотека техник (позже)                               │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│                      SUPABASE                               │
│  • users                                                    │
│  • sparring_profiles                                        │
│  • technique_matches (статистика)                          │
│  • techniques                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## WEBAPP

### Дизайн
- Gemini 3 Pro
- Цветовая палитра из COLOR-PALETTE.md

### URL-маршруты (React Router, внутри WebApp)

> Это НЕ команды бота. Это страницы внутри веб-приложения.

```
/                       → Главная (навигация)
/analyze                → Разбор борьбы
/analyze/result         → Результат разбора
/technique              → Подбор техники
/technique/result       → Результат (название + описание)
/technique/info         → Статистика: какая техника сколько мэтчей
/sparring               → Поиск партнёров (карта с геометками)
/sparring/profile/:id   → Профиль человека (при клике на геометку)
/sparring/my-profile    → Мой профиль / регистрация
/library                → Библиотека техник (заглушка, позже)
```

---

### 1. Разбор борьбы

**Путь:** `/analyze`

**Логика:** Алгоритм (без AI)

**Шаги:**
1. Пошаговый опросник (5-7 вопросов):
   - На какой руке боролся?
   - Какой стиль использовал соперник?
   - В какой фазе проиграл? (старт, середина, концовка)
   - Что чувствовал? (устал, не хватило силы, не успел среагировать)
   - и т.д.
2. Алгоритм анализирует ответы → выдаёт результат
3. Результат показывается в WebApp (`/analyze/result`)
4. Сохранение в БД (история)

**Результат содержит:**
- Что пошло не так (конкретные ошибки)
- Над чем работать
- Рекомендации по тренировке

---

### 2. Подбор техники

**Путь:** `/technique`

**Логика:** Алгоритм (без AI)

**Форма:**
- Рост
- Размах рук
- Длина предплечья
- Окружность запястья
- Сильные стороны (чекбоксы: хват, пронация, боковое давление, крюк, натяжка)

**Алгоритм:**
- Анализирует соотношения (размах/рост, предплечье/рост и т.д.)
- Учитывает сильные стороны
- Выдаёт подходящую технику

**Результат в WebApp (`/technique/result`):**
- Название техники
- Описание техники
- Почему подходит именно тебе

**Результат в Telegram Bot:**
- Видео с разбором техники
- Подпись: название + краткое описание

**Статистика (`/technique/info`):**
- Сколько людей получили какую технику (мэтчи)
- Топ-3 самых популярных техник

---

### 3. Поиск спарринг-партнёров

**Путь:** `/sparring`

**UI:** Карта (как в навигаторе)

**Логика:**
1. На карте отображаются геометки зарегистрированных армрестлеров
2. Клик на геометку → открывается профиль человека
3. В профиле есть ссылка на Telegram — люди сами списываются и договариваются

**Никаких:**
- Лайков/дизлайков
- Запросов на спарринг
- Уведомлений

**Регистрация профиля (`/sparring/my-profile`):**
- Имя, фамилия
- Город, район ЛИБО определять местоположене - то есть на выбор сайт предлагает в первую очередь определить геолокацию и ниже/сбоку в том же окне есть опция написать текстом город + район и это заносится на карту геометкой (сайт должен запрашивать разрешение на определение местоположение и сохранять на карте метку пользователя где он реально находится) для геолокации на карте. Запроси если нужно подключать апи сервис карт (четкую инструкцию пропиши как получить)
- Telegram username (обязательно, его не надо вводить, он должен подтягиваться автоматически так как пользователь из бота телеграм зашел в webapp)
- Вес (в кг/фунтах вводит что-то одно на выбор; в каждом профиле в итоге вес указывается и в кг и в фунтах)
- Рука (левша/правша/обе)
- Уровень (новичок/любитель/про) - необъективная метрика, лучше СТАЖ (придумай механику; например стаж в годах измерять)
- Стиль борьбы (аутсайд/инсайд (можешь сделать примечание что аутсайд это техники с открытым плечом - верх, клюшка и тп, а инсайд - техники с закрытым плечом - крюк, пресс и тп)) 
- О себе (опционально)
- Фото - выбор: либо автоматически (подтягивается из телеграма) и пользователь подтверждает либо может загрузить свое фото 

**Профиль партнёра (`/sparring/profile/:id`):**
- Все данные из регистрации
- Кнопка "Написать в Telegram" → ссылка `t.me/username`

---

### 4. Библиотека техник

**Путь:** `/library`

**Статус:** Заглушка (контент добавится позже)

**Структура (когда будет готово):**
- Список техник с фильтрами
- Карточка техники: название, описание, видео, когда применять

---

## TELEGRAM BOT

### Команды бота

```
/start   → Приветствие + главное меню
/profile → Профиль пользователя
/info    → Контакты
```

### Кнопки (ReplyKeyboard)

```
[ 📱 Открыть приложение ]  ← WebApp
[ 👤 Профиль ] [ ℹ️ Инфо ]
[ ⚙️ Админка ]              ← только для админа
```

### Профиль (👤)

Показывает:
- Telegram ID
- Имя
- Дата регистрации
- Тип подписки (Free / Premium)
- Количество разборов борьбы
- Ссылка на профиль спарринга (если есть)

### Инфо (ℹ️)

Показывает:
- Контакты (канал, чат)
- Статистика техник (топ мэтчей) — можно перенести сюда или оставить в WebApp

### Получение видео техники

Когда пользователь завершает подбор техники в WebApp:
1. WebApp отправляет данные в бота (через Telegram WebApp API)
2. Бот отправляет сообщение:
   - Видео техники
   - Подпись: **Название техники** + краткое описание

---

## БАЗА ДАННЫХ (Supabase)

### Таблица: users (расширить)
```sql
id                  BIGINT PRIMARY KEY
telegram_id         BIGINT UNIQUE NOT NULL
username            TEXT
first_name          TEXT
created_at          TIMESTAMP DEFAULT now()
subscription_type   TEXT DEFAULT 'free'  -- free / premium
analyze_count       INT DEFAULT 0        -- количество разборов
```

### Таблица: sparring_profiles (НОВАЯ)
```sql
id                  SERIAL PRIMARY KEY
user_id             BIGINT REFERENCES users(id)
city                TEXT NOT NULL
latitude            FLOAT NOT NULL
longitude           FLOAT NOT NULL
telegram_username   TEXT NOT NULL
weight              INT
hand                TEXT  -- left / right / both
level               TEXT  -- beginner / amateur / pro
style               TEXT  -- top_roll / hook / press / mixed
bio                 TEXT
photo_url           TEXT
is_active           BOOLEAN DEFAULT true
created_at          TIMESTAMP DEFAULT now()
```

### Таблица: analyze_history (НОВАЯ)
```sql
id                  SERIAL PRIMARY KEY
user_id             BIGINT REFERENCES users(id)
answers             JSONB NOT NULL
result              JSONB NOT NULL
created_at          TIMESTAMP DEFAULT now()
```

### Таблица: technique_matches (НОВАЯ)
```sql
id                  SERIAL PRIMARY KEY
user_id             BIGINT REFERENCES users(id)
technique_name      TEXT NOT NULL
input_data          JSONB NOT NULL  -- антропометрия
created_at          TIMESTAMP DEFAULT now()
```

### Таблица: techniques (НОВАЯ, для библиотеки)
```sql
id                  SERIAL PRIMARY KEY
name                TEXT NOT NULL
type                TEXT  -- attack / defense / counter
description         TEXT
video_url           TEXT
when_to_use         TEXT
created_at          TIMESTAMP DEFAULT now()
```

---

## ИНТЕГРАЦИЯ WEBAPP ↔ BOT

### Отправка видео техники

**WebApp (после подбора):**
```javascript
// Telegram WebApp API
window.Telegram.WebApp.sendData(JSON.stringify({
  action: 'technique_result',
  technique: 'Top Roll',
  description: 'Техника атаки через верх...',
  video_url: 'https://...'
}));
```

**Bot (получение):**
```python
@router.message(F.web_app_data)
async def handle_webapp_data(message: Message):
    data = json.loads(message.web_app_data.data)
    if data['action'] == 'technique_result':
        # Отправляем видео с подписью
        await message.answer_video(
            video=data['video_url'],
            caption=f"<b>{data['technique']}</b>\n{data['description']}",
            parse_mode=ParseMode.HTML
        )
```

---

## ФАЙЛЫ

### WebApp (новые)
```
src/pages/Analyze.tsx
src/pages/AnalyzeResult.tsx
src/pages/Technique.tsx
src/pages/TechniqueResult.tsx
src/pages/TechniqueInfo.tsx
src/pages/Sparring.tsx
src/pages/SparringProfile.tsx
src/pages/SparringMyProfile.tsx
src/pages/Library.tsx
src/components/Map.tsx
src/components/SparringMarker.tsx
src/lib/supabase.ts
src/lib/algorithms/analyzeAlgorithm.ts
src/lib/algorithms/techniqueAlgorithm.ts
```

### Bot (обновить)
```
bot/handlers/menu.py      — расширить профиль
bot/handlers/webapp.py    — обработка данных из WebApp
bot/db/models.py          — новые модели
bot/.env                  — добавить ключи API (если понадобятся)
```

---

## ПРИМЕЧАНИЯ

1. **AI не используется** — вся логика на алгоритмах
2. **Карта** — использовать Leaflet.js или Mapbox (бесплатный тариф)
3. **Видео техник** — пока заглушки, контент добавится позже
4. **Монетизация** — после MVP (Premium подписка)

---

## ГОТОВО К РЕАЛИЗАЦИИ ✅
